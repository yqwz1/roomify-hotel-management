#!/usr/bin/env sh
# Lightweight wrapper: uses Maven if installed.
if command -v mvn >/dev/null 2>&1; then
  mvn "$@"
else
  echo "Maven (mvn) is not installed. Install Maven or add a full Maven Wrapper."
  exit 1
fi
